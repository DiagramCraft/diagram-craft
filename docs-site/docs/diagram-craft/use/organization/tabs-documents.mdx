---
sidebar_position: 2
related_reading:
  - label: Layers
    to: /diagram-craft/diagram-craft/use/organization/layers
---

import ThemedImage from '@theme/ThemedImage';

# Tabs and Documents

Each Diagram Craft document can contain multiple diagrams. The tab strip provides quick access to them, while the **Document** tab in the **Structure** window exposes their complete hierarchy.

<ThemedImage
  alt="Diagram Craft tab strip showing multiple top-level diagrams and an active nested subpage"
  sources={{
    light: require('/img/diagram-craft/organization/tabs-documents-light.png').default,
    dark: require('/img/diagram-craft/organization/tabs-documents-dark.png').default,
  }}
/>

## Add And Select Diagrams

Use the add action beside the tab strip to create a diagram. Select its tab to make it active; the canvas, active layer, and relevant tool windows update to the selected diagram.

## Create Nested Diagrams

Diagram Craft supports nested diagrams. That is useful when one top-level diagram owns a small family of supporting views.

Open a diagram's context menu and choose **Add subpage** to create a nested diagram. Nested diagrams remain independent canvases; nesting changes document navigation rather than grouping their elements.

## Manage The Document Tree

Use the **Document** tab in the Structure window to navigate, rename, delete, and reorder diagrams. Drag-and-drop reordering works between diagrams with the same parent. The tree exposes nested diagrams even when the tab strip cannot show the full hierarchy at once.

## Use The Structure Window

The Structure window provides two views for managing a document:

- **Layer** shows the active diagram's layer stack and element tree.
- **Document** shows the hierarchy of diagrams and subpages across the document.

Expand tree items to inspect nested diagrams, layers, groups, and elements. Selecting an entry navigates to or selects the corresponding document object, which is useful when it is difficult to locate directly on the canvas.

Use the **Document** view when you need to move between overview and detail pages, turn a diagram into a subpage, or reorder diagrams to match the document's story. Use the **Layer** view when a crowded diagram makes direct selection difficult or when you need to lock, hide, or inspect elements in the active diagram.

<ThemedImage
  alt="Diagram Craft structure panel showing the document hierarchy for multiple diagrams and subpages"
  sources={{
    light: require('/img/diagram-craft/organization/document-structure-light.png').default,
    dark: require('/img/diagram-craft/organization/document-structure-dark.png').default,
  }}
/>
