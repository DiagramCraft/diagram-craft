# API And Integrations

Arch Register's HTTP API is defined as oRPC contracts and exported as an OpenAPI 3.1 specification. The full,
generated endpoint-by-endpoint reference is published as a static [API Reference](pathname:///arch-register/api.html)
page, rendered with [Redoc](https://github.com/Redocly/redoc) and grouped by domain via the spec's operation tags
(Enums, Schemas, Entities, Projects, and so on). This page covers where that reference comes from and how to keep
it up to date.

## API surfaces

New consumers should choose the surface that matches their role:

- `/api/application/v1/*` is the first-party application surface. It is being introduced domain by domain while
  existing `/api/*` routes remain available for compatibility.
- `/api/integrations/v1/*` is the stable REST surface for supported third-party integrations. The entity sync routes
  use `/entities/{id}` for internal IDs and `/entities/byExternalKey/{source}/{externalKey}` for external identities.
  API catalog integrations use `/api-specifications/byExternalKey/{source}/{externalKey}` to atomically sync an
  API entity and a provider-scoped specification source, with `/refresh` for URL-backed refreshes.
- `/api/adapters/diagram-craft/*` is the Diagram Craft-specific adapter surface. The existing `/api/public/*` paths are
  authenticated compatibility aliases; `public` is not a security boundary.
- `/mcp` is a separate MCP protocol endpoint, documented under [MCP Server](mcp-server), rather than a REST path.

The legacy `/api/*` surface is not removed as part of the initial migration. Clients should move when their domain has
an equivalent versioned contract, and breaking changes to versioned surfaces require a new major URL version.

## Current route inventory

The route classification below is the compatibility baseline for the migration. “Live” means the path is currently
served by the application; “target” means the path is the destination for a later migration issue.

| Surface | Current routes | Status | Owner / migration issue |
| --- | --- | --- | --- |
| Legacy application API | `/api/workspaces`, `/api/:workspace/*` for catalog, projects, configuration, governance, search, audit, jobs, documents, webhooks, and related domains | Live canonical routes | Keep compatible while domains move to the application surface; inventory changes belong in #2448 |
| Application v1 | `/api/application/v1/:workspace/schemas/*` and `/api/application/v1/:workspace/data/*` | Live aliases for catalog schemas and entities | Extend domain by domain in #2446 |
| Integration v1 | `GET /api/integrations/v1/:workspace/schemas`, `/relation-schemas`, `/relations`, `/data/:id/typed-relations`, `/entities/*`, and `/api-specifications/byExternalKey/*` | Live schema, typed-relation, entity-sync, and API-specification sync routes | Productize the supported contract and migrate remaining examples in #2449 |
| Diagram Craft adapter | `/api/adapters/diagram-craft/:workspace/schemas` and `/api/adapters/diagram-craft/:workspace/data` | Live aliases for the authenticated `/api/public/*` adapter routes | Expand and document adapter-specific behavior in #2451 |
| Legacy adapter name | `/api/public/:workspace/*` | Live compatibility alias; the name does not make the route public | Retain until all Diagram Craft consumers use the adapter surface |
| Authentication infrastructure | `/api/auth/*`, `/api/auth/oidc/callback` | Live special-purpose browser and session/token endpoints | Remains outside application/integration versioning; token audience enforcement is deferred to #2383 |
| Development infrastructure | `/api/dev/*` | Live, environment-gated development endpoints | Never expose as a supported integration surface |
| MCP | `/mcp`, legacy `/sse`, `/messages` | Live in the standalone MCP server | Keep as a protocol surface; upstream REST migration is #2450 |
| API descriptions | `/openapi.yaml`, `/openapi.json`, and surface-specific `/openapi/*.json` endpoints | Live documentation endpoints | Keep unified output compatible; publish surface specs in #2447 |

The inventory intentionally distinguishes a route that exists from a route that has been selected as a migration
destination. Surface ownership is not exclusive: a contract may be exposed on both the application and integration
surfaces when the behavior, authorization, and representation are the same. In particular, `/api/integrations/v1/*`
must not be treated as generally available until #2449 mounts the entity-sync contract there.

## Compatibility policy

- Existing `/api/*` routes are the compatibility baseline. A migration adds a new route alias or canonical route before
  changing any existing consumer.
- A domain may be aliased on both `/api/application/v1/*` and `/api/integrations/v1/*` when both consumers need the same
  contract. Choose one primary owner for documentation and lifecycle decisions, and record the second path as an
  intentional alias rather than maintaining separate business logic.
- An alias must call the same contract handler and authorization path as its canonical route. It must not duplicate
  business logic or weaken authentication.
- Aliases preserve HTTP methods, request and response shapes, error behavior, and path parameters. The only permitted
  transformation is the URL prefix mapping documented for that surface.
- New public contracts use `/v{major}` in the surface path. Additive changes are allowed within a major version;
  breaking changes require a new major version and a migration note.
- `/api/public/*` is a naming-compatibility path, not an anonymous API. Authentication and workspace permissions apply
  exactly as they do to the adapter route.
- Do not add token audiences, scopes, or enforcement as part of route migration. Those decisions are tracked
  separately in #2383.

## Deprecation and removal

When a replacement is live, update consumers and docs first, then mark the old path as compatibility-only. Removal of a
legacy path requires all of the following:

1. the replacement has a published contract and OpenAPI description;
2. first-party consumers have used the replacement for one release cycle;
3. external consumers have a migration note and a warning period;
4. request metrics show no material usage of the old path, or remaining consumers have an agreed migration owner; and
5. the removal is called out in release notes and covered by a regression test proving the replacement remains live.

Until those criteria are met, compatibility aliases remain supported and are not silently redirected, since redirects can
change methods, authentication handling, or streaming behavior.

## Primary Sources

- server OpenAPI specification, generated to `arch-register-packages/server/openapi.json`
- oRPC contract definitions in `arch-register-packages/api-types`
- unified spec assembly in `arch-register-packages/server/src/openapi.ts`
- application surface specification at `/openapi/application-v1.json`
- integration surface specification at `/openapi/integrations-v1.json`
- Diagram Craft adapter specification at `/openapi/adapters/diagram-craft.json`

## Updating The API Reference

The [API Reference](pathname:///arch-register/api.html) page is a static HTML file built from the committed
`openapi.json` file, not derived live from source. After changing an oRPC contract in
`arch-register-packages/api-types`:

1. Regenerate the spec and commit the result:
   ```bash
   pnpm --filter @arch-register/server openapi:generate
   ```
2. That's it — the docs site rebuilds the static API reference page from the committed spec as part of
   `pnpm docs:dev` and `pnpm docs:build` (a fast standalone bundling step, not a slow regeneration process, so it
   doesn't meaningfully affect dev server startup time). A CI check
   (`pnpm --filter @arch-register/server openapi:check`) fails the build if `openapi.json` is out of sync with the
   contracts, so a forgotten regeneration step won't silently go stale.

## Use This Page For

- finding the contract source of truth
- understanding where integration-level docs live
- the developer workflow for updating the generated API reference

## Other Integration Surfaces

Besides the HTTP API:

- the [MCP Server](mcp-server) exposes entity and typed-relation discovery, traversal, and mutation tools for AI and automation clients
- outbound [webhooks](../admin/workspace-settings#administration) deliver workspace events to external
  systems
- the [External Integration Example](external-integrations) demonstrates a complete webhook-to-API
  write-back service

### API specification synchronization

The API specification sync contract accepts a complete entity mutation together with one source state. A source has a
provider-scoped `sourceKey`, independent of internal Arch Register IDs, and can be a bounded submitted document, a
secure HTTPS URL, or a link-only source. Submitted documents and URL fetches use SHA-256 checksums to avoid duplicate
immutable revisions. URL refreshes run through the existing job worker; scheduled URL policies use the same handler as
manual refreshes. A missing source state preserves the entity and last successful revision while exposing a stale or
failed diagnostic, and raw specification content remains protected by `artifact.content.view`.

## Related Pages

- [API Reference](pathname:///arch-register/api.html)
- [MCP Server](mcp-server)
- [Export and Import](../admin/export-import)
